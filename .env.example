# Example environment configuration for the trading journal Flask app

# Secret key used by Flask to secure sessions. Replace with a long, random string
# in production. For local development you can leave this as is.
SECRET_KEY=super-secret-key

# Database connection URI. Defaults to a SQLite file in the project root.
# To use PostgreSQL or another database in production, set this to something like:
# DATABASE_URL=postgresql://username:password@hostname:port/database
DATABASE_URL=sqlite:///journal.db
